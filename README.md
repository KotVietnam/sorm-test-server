### 🏢 DLP Lab Server (Infrastructure)

Серверная часть ("Жертва") предназначена для комплексного тестирования систем защиты информации (DLP/DPI).
Проект разворачивает в Docker изолированную среду с уязвимыми сервисами — FTP, SMTP, SIP, Radius и Telnet — для приёма тестового трафика.

### 📋 Требования

ОС: Linux (Ubuntu/Debian/CentOS) или Windows (через WSL2)

Docker: установлен и запущен

Docker Compose: установлен

### 🚀 Установка и запуск

1. Запуск

В папке с файлом docker-compose.yml выполните команду:
```bash
docker-compose up -d
```
2. Проверка

Убедитесь, что контейнеры запущены:
```bash
docker-compose ps
```
3. Узнать IP‑адрес

## ⚠️ Важно: узнайте IP‑адрес этого сервера. Он понадобится для настройки клиента.

# Linux
```bash
hostname -I
```
# Windows (WSL)
```bash
ip addr show
```

Запишите этот IP (например, 192.168.1.50).

### 🛠 Доступные сервисы и порты

DLP‑система должна видеть трафик на следующих портах:

Сервис	Протокол	Порт	Логин / Пароль
FTP	TCP	21	dlpuser / dlpsecret
SMTP	TCP	1025	(без авторизации)
Web UI (Mail)	HTTP	8025	(просмотр писем)
SIP (VoIP)	UDP/TCP	5060	(Asterisk)
Radius	UDP	1812	Secret: testing123
Telnet	TCP	23	user / pass
Web	HTTP	80	(Nginx)

###🛑 Остановка

Чтобы остановить все сервисы, выполните:
```bash
docker-compose down
```
